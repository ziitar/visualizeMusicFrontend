import { FormItemDirective } from './form-item-content.directive';

describe('FormItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FormItemDirective();
    expect(directive).toBeTruthy();
  });
});
