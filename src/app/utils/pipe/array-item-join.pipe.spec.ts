import { ArrayItemJoinPipe } from './array-item-join.pipe';

describe('ArrayItemJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayItemJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
